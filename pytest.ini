[pytest]
asyncio_mode = auto
addopts = --strict-markers
testpaths =
    tests
    scripts/tests
    vault/tests
markers =
    integration: requires live services, a running server, or external resources
    slow: downloads or executes large local models
